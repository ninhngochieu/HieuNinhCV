# /autoplan Review — HieuNinhCV (frontend-developer lens, 2nd pass)

**Branch:** master **Commit:** 810efc9 **Mode:** single-reviewer (Codex/subagent bins absent → degradation)
**Viewpoint:** frontend developer — broken assets, a11y, CSS correctness, dead code, perf, dependency hygiene.

---

## Phase 0 — Intake
Read: `globals.css`, `page.module.css`, `PortfolioSection.tsx`, `page.tsx`, `layout.tsx`, `data/portfolio.json`, `package.json`, `public/`. Build green (verified). `next lint` not yet run.

## Phase 1 — CEO (strategic)
Premise (frontend-only) sound. No scope change. Deferred items from prior pass (D5 font network, PB password rotate) still stand.

## Phase 2 — Design (UI/UX, frontend lens)
- **F1 (broken asset, BUG):** `portfolio.json` projects reference `imageUrl: "/projects/crm.png"` & `"/projects/medication.png"` but `public/projects/` does NOT exist → 404 if ever rendered. Projects section currently renders text only (imageUrl unused), so not user-visible yet, but the data contract is wrong and will break the moment an `<img>` is added. **Auto-fix (P5):** either add the assets or null out `imageUrl` to keep the contract honest. Decision: null out imageUrl (no real images available; avoids shipping 404s). EXECUTE.
- **F2 (dead CSS):** `page.module.css` is the default Next scaffold (light theme `.page/.main/.intro`) — **imported nowhere** (page.tsx uses inline + global classes). Confusing dead file. **Auto-fix (P4 DRY / P5):** delete it. EXECUTE.
- **F3 (stale dep):** `package.json` still lists `@infisical/sdk` though Infisical/backend removed. Dead dependency → larger install, unused import surface. **Auto-fix (P5):** remove from package.json. EXECUTE. (node_modules already installed; lockfile prune on next `npm ci`.)
- **F4 (CSS bug):** `globals.css:116` `.glass-card:hover { border-borderColor: var(--glass-highlight); }` — `border-borderColor` is not a valid CSS property (typo for `border-color`). Silent no-op → hover border-color change never applies. **Auto-fix (P5):** rename to `border-color`. EXECUTE.

## Phase 3 — Eng (architecture/edge/security)
- **F5 (a11y):** Contact/CTA icons are `<a>` with only an SVG child and `title`; icons lack `aria-hidden`, and links rely on `title` for meaning (not announced consistently). **Auto-fix (P1 completeness):** add `aria-label` to each contact link + `aria-hidden` to decorative SVGs. EXECUTE (low risk, in blast radius).
- **F6 (perf / render-blocking font):** `globals.css:1` `@import url('https://fonts.googleapis.com/...Inter+Outfit')` is a runtime, render-blocking, non-Next-optimized fetch — AND `layout.tsx` separately loads Geist via `next/font/google`. Two font systems, one unoptimized. **Auto-fix (P3 pragmatic):** remove the `@import` line (Geist from next/font already covers the app; the Inter/Outfit `@import` is unused since no element sets `--font-inter`/`--font-outfit` body font except `body{font-family:var(--font-inter)}` which then falls back to system-ui). Decision: drop the `@import` to kill the network round-trip; keep `--font-inter/--font-outfit` vars (still resolve to system-ui fallback). EXECUTE.
- **F7 (maintainability):** All styling is inline `style={{...}}` with magic strings ('8rem', 'var(--primary)'). Hard to theme/maintain but consistent with current approach; not a bug. Noted, not changed (would be a larger refactor — out of scope/blast radius).
- Security: clean (no secrets, no backend). ✅

## Phase 4 — DX
- `npm run lint` should be run post-fix (eslint-config-next). Add to verify. F3 removal keeps CI lean.
- Onboarding: deleting dead `page.module.css` + Infisical dep reduces confusion. ✅

---

## AUTO-DECISIONS
| # | Issue | Principle | Decision | Exec? |
|---|---|---|---|---|
| F1 | projects imageUrl → 404 | P5 | null out imageUrl (no assets) | ✅ |
| F2 | dead page.module.css | P4/P5 | delete | ✅ |
| F3 | @infisical/sdk unused | P5 | remove from package.json | ✅ |
| F4 | border-borderColor typo | P5 | → border-color | ✅ |
| F5 | a11y contact links | P1 | add aria-label + aria-hidden | ✅ |
| F6 | @import google fonts render-blocking | P3 | remove @import | ✅ |
| F7 | inline-style magic strings | — | defer (larger refactor) | ⏸ |

## Gate: none blocking. All mechanical.

## Deferred (user / larger)
- F7 inline→CSS modules refactor (taste, larger).
- Real project images (user provides → re-add imageUrl).
- PB password rotate (user action).
