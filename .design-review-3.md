# /design-review — HieuNinhCV (round 3 — A-grade Editorial/Swiss redesign)

**Target:** http://localhost:4173/ (static export, this round)
**Method:** rendered-site audit (browser screenshots + DOM evidence; console 0 errors).

## Dual Headline Scores
- **Design Score: A** (editorial typographic POV, deliberate, not template)
- **AI Slop Score: A** (no default dark-dev tropes; serif display + amber + asymmetric grid)

## What changed (full redesign, hướng 2)
- **Typography:** added **Fraunces** (display serif) via next/font; hero name set in heavy
  serif (clamp 3–7.5rem, -0.03em tracking) — strong typographic focal point.
- **Accent:** cyan → **amber `#f59e0b`** (single accent, breaks the cyan cliche).
- **Layout:** CSS Modules replace inline styles. Sections use an **asymmetric editorial
  grid** (200px label left, content right). Hero is a left-aligned serif block + meta row.
- **Cards:** dropped glass-morphism; now 1px hairline border + flat 2% white surface.
- **Motion:** reduced glow on CTA to fit Swiss restraint; reduced-motion honored.
- **Contrast:** fg `#f5f3ee` on `#0b0b0f` — excellent; secondary `#b9b6ad`.

## Re-audit (browser-verified)
- Hero: large display serif, dominant ✅
- Amber accent visible on title/company/CTA, no cyan ✅
- Sections asymmetric (label left, content right) ✅
- Contrast/readability excellent ✅
- Vision: "does not look like generic AI slop; typography and spacing too deliberate" ✅

## Residual (non-blocking)
- Content bullets still slightly "resume-bot" generic — content ownable, not a design flaw.
- Date 08/2025 flagged by vision as future, but current context is 2026-08-08 → valid past date.
- Minor: CTA glow reduced but still a touch SaaS vs strict Swiss — acceptable tradeoff for CTA visibility.

STATUS: **DONE** — A-grade redesign executed and verified.
