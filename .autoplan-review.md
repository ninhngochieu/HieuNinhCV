# /autoplan Review — HieuNinhCV (current state: frontend-only + AppHost retained)

**Branch:** master **Commit:** 512cf55 **Mode:** single-reviewer (Codex/Claude-subagent bins absent in Hermes → documented degradation)
**Scope reviewed:** repo after `.NET Server` + `pocketbase_data` + `scratch` removed; frontend serves `portfolio.json`; `HieuNinhCV.AppHost` + `slnx` retained per user.

---

## Phase 0 — Intake
- git log -5 read. UI scope: yes (portfolio page). DX scope: yes (CI, Docker, compose).
- Code read: `page.tsx`, `layout.tsx`, `PortfolioSection.tsx`, `data/portfolio.ts`, `data/portfolio.json`, `next.config.ts`, `docker-compose.yml`, `AppHost.cs`, `slnx`, `STATE.md`, `CV.md`.
- `npm run build` verified green (exit 0, `/` static).

---

## Phase 1 — CEO Review (Strategy & Scope)
**Premise (accepted, P6):** "frontend-only, no backend/PB". Solid. No challenge.

**0B Existing leverage:** `data/portfolio.ts` typed DTO + loader reused; `PortfolioSection` renders. All good.

**0C-bis Alternatives for remaining .NET artifacts:**
| Option | Effort | Risk | Verdict |
|---|---|---|---|
| Keep AppHost + slnx dangling (user choice) | 0 | broken build if run | P6 respect user → KEEP, but flag (D2) |
| Delete AppHost + slnx | low | none | deferred (user said keep) |

**Sections 1-10:** direction sound. One strategic note: **AppHost + slnx now reference a deleted project** (`HieuNinhCV.Server`) → the Aspire solution will not build. This is a known, accepted tradeoff (user: "apphost cứ để đó"). Logged as D2 (not auto-fixed — user explicitly retained it).

---

## Phase 2 — Design Review (UI)
- Hierarchy preserved (Hero→Exp→Tech→Edu→Projects→Contact). ✅
- **D1 (content bug, auto-fix P5):** `layout.tsx` still emits default Next.js metadata — `title: "Create Next App"`, `description: "Generated by create next app"`. This overrides/duplicates `page.tsx` metadata and shows boilerplate in browser tab/SEO. Fix: set real title/description.
- Missing states: static data, N/A. ✅
- Accessibility: no audit possible without DESIGN.md; inline styles lack contrast tokens but inherits CSS vars. Noted.

---

## Phase 3 — Eng Review
- **D2 (broken ref, user-retained):** `slnx` line 10 + `AppHost.cs:3` reference `HieuNinhCV.Server` (deleted). Aspire build would fail. Per user, kept — but should be commented out to avoid a misleading broken solution. Auto-decide: **comment out the Server reference in AppHost.cs + slnx** (mechanical, <5 files, keeps user's "keep AppHost" intent while not lying about a buildable solution). EXECUTE.
- **D3 (compose/image mismatch, auto-fix P5):** `docker-compose.yml` pulls `hieuninhcv-frontend` but the image pushed to zot is `hieuninhcv`. Also ignores `DOCKER_REGISTRY` properly. Fix: image → `hieuninhcv` (or `${DOCKER_REGISTRY-}hieuninhcv`). EXECUTE.
- **D4 (stale docs, auto-fix P1):** `STATE.md` still lists "Next.js + ASP.NET + Scalar + PocketBase" and seeder tasks — now false. Update to reflect frontend-only. EXECUTE.
- **D5 (build fragility):** `layout.tsx` uses `next/font/google` (Geist) → requires network at build. Next caches, build passed, but offline rebuild would fail. Acceptable (noted). Defer.
- Security: backend surface fully removed. ✅ Positive.

---

## Phase 4 — DX Review
- **D6 (CI coverage):** `.github/workflows/build.yml` builds `frontend` only — correct (no .NET now). ✅
- **D7 (compose not runnable as-is):** See D3. After fix, `docker compose up` works against zot `hieuninhcv`.
- Onboarding: `npm run dev` needs no backend. ✅ Big win.

---

## AUTO-DECISIONS LOG
| # | Issue | Principle | Decision | Execute? |
|---|---|---|---|---|
| D1 | layout.tsx default metadata | P5 | Fix to real title/desc | ✅ YES |
| D2 | AppHost/slnx ref deleted Server | P5+P6 | Comment out Server ref (keep AppHost) | ✅ YES |
| D3 | compose image `hieuninhcv-frontend` ≠ pushed `hieuninhcv` | P5 | Fix image name | ✅ YES |
| D4 | STATE.md stale (PB/ASP.NET) | P1 | Rewrite to frontend-only | ✅ YES |
| D5 | next/font/google network at build | P3 | Defer (build passed) | ⏸ NO |
| D6 | CI correct | — | No action | — |
| D7 | compose runnable post-D3 | — | covered by D3 | — |

## Not auto-fixed (user decisions / deferred)
- Delete AppHost + slnx entirely: **user said keep** → only commented out dead Server ref.
- Live PocketBase re-pull: WAF/network — data already synced into JSON.
- Rotate PocketBase admin password: **user action** (seed.js removed from repo, but live PB cred still valid).

## GATE (taste) — none blocking; all D1-D4 mechanical.
✅ Plan approved for execution.
