# /autoplan Review — HieuNinhCV Frontend-Only Pivot

**Branch:** master **Commit:** f9d7374 **Mode:** single-reviewer (Codex/Claude-subagent bins absent in Hermes → documented degradation)
**Plan:** Cut .NET backend + PocketBase; serve portfolio from local structured JSON (sourced from PocketBase content via the seeder `scratch/seed.js`, which is the verbatim record set written into PocketBase).

---

## Phase 0 — Intake
- git log -30 read; repo = .NET 10 API (HieuNinhCV.Server) + Aspire AppHost + PocketBase data + Next.js 16 frontend.
- UI scope detected: yes (portfolio page, components). DX scope detected: yes (API→JSON migration, Docker, CI).
- Network note: this sandbox is WAF-blocked from `pocketbase.ninhngochieu.online` (HTTP 403, Cloudflare 1010). Live API pull impossible from here. Data provenance = `scratch/seed.js` (authoritative seed written to PocketBase).

### Restore point
`git stash` not needed; working tree is the plan. Restore = `git checkout -- frontend/src/data frontend/src/app frontend/next.config.ts next.config/docker-compose.yml`.

---

## Phase 1 — CEO Review (Strategy & Scope)

**Premise gate (user direction, accepted — P6):** "Chỉ dùng frontend, bỏ backend/PocketBase, data từ JSON có cấu trúc (gốc của tôi)." Accepted as the problem to solve. No challenge.

**0B Existing code leverage:**
| Sub-problem | Existing code reused |
|---|---|
| Typed data model | `PortfolioSection.tsx` interfaces → extracted to `data/portfolio.ts` |
| Loader | new `getPortfolioData()` (replaces 5 `fetch('/api/...')`) |
| Render | `PortfolioSection.tsx` kept, de-backendified |

**0C Dream state:** CURRENT (Aspire+API+PB) → THIS PLAN (Next.js standalone + local JSON) → 12-MO (static export / CDN, zero server).

**0C-bis Alternatives:**
| Approach | Effort | Risk | Verdict |
|---|---|---|---|
| Local JSON imported at build (chosen) | low | none | P1 completeness, P5 explicit → APPROVE |
| Keep /api route that reads JSON file | med | extra surface | P4 DRY reject |
| Fetch JSON at runtime from /public | med | extra fetch, no SSR | P3 pragmatic → defer |

**Sections 1-10 findings (auto-decided):**
1. *Completeness* — All 5 collections (bio/skills/exp/edu/projects) captured in JSON. ✅
2. *Scope* — Blast radius = frontend/. Backend projects (`Server`, `AppHost`, `slnx`, `pocketbase_data`, `scratch`) are dead weight → within "boil lakes" (P2) but large (not <5 files / <1 day). Mark **TASTE** (see Gate).
3. *Risk* — Secrets in `scratch/seed.js` (PB admin pw) + `Program.cs` (already rotated). Keep out of new code. ✅ (no new secret introduced)
4-10. No further strategic concerns.

**CEO consensus (single-reviewer):** direction sound; the only open scope question is whether to physically delete backend dirs.

---

## Phase 2 — Design Review (UI)
- Information hierarchy: Hero → Experience → Tech Stack → Education → Contact. Preserved. ✅
- Missing states: original had `loading` + `error` from fetch; **now data is static/SSR so those states are obsolete** — auto-fixed (P5) by removing the fetch/loading branch. Empty-data handled with `if (!bio) return …`.
- `projects` collected in data + typed but **never rendered** in `PortfolioSection.tsx` (pre-existing gap). Auto-decide (P1 completeness): **render a Projects section** — within blast radius, <5 files. APPROVE as part of execution.
- No DESIGN.md present → no system-alignment check possible. Noted.

---

## Phase 3 — Eng Review
- *Architecture* — `import('./portfolio.json')` at build = zero runtime deps, zero API surface. ✅
- *Edge cases* — `{YEARS_EXP}` token expanded at render (kept). Empty arrays render nothing (safe). ✅
- *Tests* — No test suite exists; adding one is out of scope per "execute the pivot". Deferred to TODOS.
- *Security* — **Net reduction**: removes Infisical Universal Auth + PocketBase superuser surface entirely. ✅ (Positive finding)
- *Hidden complexity* — `next.config.ts` still had `/api` rewrite to dead server → removed. `docker-compose.yml` still referenced backend → simplified to frontend-only.
- *Bug fix found* — `PortfolioSection` imported `useEffect` (unused post-change) — cleaned to `useState`.

---

## Phase 4 — DX Review
- *Dev onboarding* — `npm run dev` now needs no PocketBase/Aspire. ✅ Big DX win.
- *CI* — `.github/workflows` absent → added a minimal `build.yml` (node 22, `npm ci`, `npm run build`) so the repo is verifiable. Within blast radius, <5 files. APPROVE (P2 boil lakes).
- *Docker* — `Dockerfile` already standalone; compose simplified.

---

## AUTO-DECISIONS LOG
| # | Issue | Principle | Decision |
|---|---|---|---|
| A1 | Delete backend dirs (Server/AppHost/slnx/pb_data/scratch) | P2 vs scope | TASTE → ask user at gate |
| A2 | Render Projects section (data exists, unrendered) | P1 | EXECUTE |
| A3 | Add minimal CI build.yml | P2 | EXECUTE |
| A4 | Remove /api rewrite + simplify compose | P5 | EXECUTE (done) |
| A5 | Auth-rotate PocketBase admin (scratch pw leaked) | security | OUT OF SCOPE (sandbox WAF-blocked; flagged) |

## GATE — Taste / User-Challenge
**A1 (scope):** Physically delete the .NET backend + `pocketbase_data` + `scratch` from the repo?
- YES (recommended): repo becomes pure frontend, smaller, no dead code. ~7 files/dirs.
- NO: leave them (gitignored/untouched), only frontend changed.
*This is the one decision I will NOT auto-decide — it's a structural choice about your repo. Everything else executes now.*

## Not in scope (deferred)
- CI test suite, admin CRUD (dropped with backend), live PocketBase re-pull (WAF-blocked here).
